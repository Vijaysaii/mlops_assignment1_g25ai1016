name: ML Model CI

on:
  push:
    branches:
      - kernelridge

jobs:
  run-models:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Decision Tree model
        run: |
          python train.py

      - name: Run KernelRidge model
        run: |
          python train2.py
